apiVersion: batch.math.operator.com/v1
kind: Operator
metadata:
  labels:
    app.kubernetes.io/name: math-operator
    app.kubernetes.io/managed-by: kustomize
  name: operator-sample
spec:
  # TODO(user): Add fields here
